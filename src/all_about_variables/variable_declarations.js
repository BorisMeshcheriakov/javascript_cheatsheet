/**
 * @module all_about_variables/variable_declarations
 *
 * @description Для использования переменных используютсяя ключевые слова var, let и const.
 */

/**
 * Переменная var.
 *
 * По умолчанию имеет значение undefined.
 *
 * Var может иметь отношение к глобальной или функциональной области видимости.
 */

var testVar = "test var";
var testVarNovalue; // undefined

/**
 * Переменная const.
 *
 * Обязательно требует присвоить значение.
 *
 * Const может иметь отношение к глобальной, блочной или функциональной области видимости.
 *
 */

const testConst = "test const2";

let testLet = "test let";
